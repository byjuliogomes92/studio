rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Apenas usuários autenticados podem ler e escrever
    // A segurança por workspace será aplicada via path
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
